#!/usr/bin/make -f

export DH_VERBOSE=1
export DH_VIRTUALENV_ARGUMENTS=--python=/usr/bin/python3
export DH_UPGRADE_SETUPTOOLS=
export DH_PIP_EXTRA_ARGS=--no-use-wheel

%:
	dh $@ --with python3 --buildsystem dh_virtualenv
